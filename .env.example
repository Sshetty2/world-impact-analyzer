# Get your OpenAI API Key here: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=****

# Optional: Override the default OpenAI model for analysis (defaults to gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Override the default OpenAI model for Wikipedia summarization (defaults to gpt-4o-mini)
# SUMMARIZATION_MODEL=gpt-4o-mini

# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on vercel

# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a database here: https://vercel.com/docs/storage/vercel-postgres/quickstart
# This is used by the app at runtime
POSTGRES_URL=****

# This is used by database migrations (should be the same as POSTGRES_URL)
# DATABASE_URL is loaded by drizzle-kit and the migration script
DATABASE_URL=****
